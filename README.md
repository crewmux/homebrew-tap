# CrewMux Homebrew Tap

```bash
brew tap crewmux/tap
brew install --HEAD crewmux/tap/crewmux
```

Stable installs will use:

```bash
brew install crewmux/tap/crewmux
```

Until the first tagged release is published, `--HEAD` is required.
